
version = 1.0.3

workspace.lib.dir = ../lib
workspace.dist.dir = ../dist

classes.dir = ${basedir}/classes
src.dir = ${basedir}/src
gen.dir = ${basedir}/gen
test.dir = ${basedir}/test
test.gen.dir = ${basedir}/test-gen
lib.dir = ${workspace.lib.dir}
dist.dir = ${workspace.dist.dir}

module = plazma-framework-uwt
module.client = plazma-framework-uwt-client

module.jar = ${module}-${version}.jar
module.client.jar = ${module.client}-${version}.jar

module.source.jar = ${module}-${version}-source.jar
module.client.source.jar = ${module.client}-${version}-source.jar
